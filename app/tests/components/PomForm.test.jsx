import expect from 'expect';
import React from 'react';
import ReactDOM from 'react-dom';
import TestUtils from 'react-addons-test-utils';
import $ from 'jQuery';

import PomForm from 'PomForm'

describe('PomForm', () => {
    it('Should exist', () => {
        expect(PomForm).toExist();
    })
})